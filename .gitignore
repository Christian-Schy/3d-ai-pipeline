# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Generated 3D models
data/output/

# Runtime / session data
data/sessions/
data/rag_db/

# Logs
*.log

# Temp STL files
/tmp/*.stl
/output.stl

# IDE / OS
.DS_Store
.claude/

# Local credentials (never commit)
config/bambu.json
.env
.env.local

# DSPy outputs: experimental runs + .bak files stay local; adopted/stable
# artifacts (heatmap-verified) are versioned so a fresh checkout has them active.
data/dspy_optimized/*
!data/dspy_optimized/hole_classifier_optimized.json
!data/dspy_optimized/pocket_classifier_optimized.json
!data/dspy_optimized/slot_classifier_optimized.json
!data/dspy_optimized/grid_classifier_optimized.json
!data/dspy_optimized/circular_classifier_optimized.json
!data/dspy_optimized/linear_classifier_optimized.json
!data/dspy_optimized/edge_feature_classifier_optimized.json
!data/dspy_optimized/inventar_optimized.json
!data/dspy_optimized/platzierer_frame_optimized.json
!data/dspy_optimized/platzierer_alignment_optimized.json
!data/dspy_optimized/platzierer_anchor_optimized.json
!data/dspy_optimized/platzierer_offset_optimized.json
# W6 — KNN-Demo-Pools (Laufzeit-Artefakt von scripts/build_classifier_demo_pools.py).
# Der DemoRetriever liest diese Dateien — muessen im Repo sein.
!data/dspy_optimized/hole_classifier_demo_pool.json
!data/dspy_optimized/pocket_classifier_demo_pool.json
!data/dspy_optimized/slot_classifier_demo_pool.json
!data/dspy_optimized/grid_classifier_demo_pool.json
!data/dspy_optimized/circular_classifier_demo_pool.json
!data/dspy_optimized/linear_classifier_demo_pool.json
!data/dspy_optimized/edge_feature_classifier_demo_pool.json
# NOT versioned: aktions_klassifizierer_optimized.json (Monolith, disabled wegen
# Tasche-Cross-Contamination), position_extractor_optimized.json (rolled back),
# platzierer_optimized.json (Legacy-Monolith), blueprint_architect/assembly/
# feature_definierer/teil_definierer (Legacy oder unbenutzt), *.bak* (Backups).

# Internal cleanup planning files
CLEANUP_PLAN.md

# Local archives
_archive/
